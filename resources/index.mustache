<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>{{ setting.author.name }}</title>
  <!-- UIkit CSS -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/uikit/3.0.0-beta.35/css/uikit.min.css"/>
  <link rel="stylesheet" href="resources/assets/css/theme.css">

  <!-- UIkit JS -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/uikit/3.0.0-beta.35/js/uikit.min.js" defer></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/uikit/3.0.0-beta.35/js/uikit-icons.min.js" defer></script>
</head>
<body>
  <div class="uk-section uk-section-small uk-flex uk-flex-center uk-flex-middle@m" uk-height-viewport>
      <div class="uk-card uk-card-default uk-width-1-3@m">
        {{ #setting.author.cover }}
        <div class="uk-card-media-top uk-visible@m">
          <div class="uk-cover-container uk-height-medium">
              <img src="{{.}}" alt="" uk-cover>
          </div>
        </div>
        {{ /setting.author.cover }}
        <div class="uk-card-body">
          <div uk-grid class="uk-flex uk-flex-center uk-flex-left@m">
            {{ #setting.author.avatar }}
            <div>
              <img class="uk-card-avatar uk-border-circle" src="{{{.}}}">
            </div>
            {{ /setting.author.avatar }}
            <div>
              <div class="uk-card-title">{{ setting.author.name }}</div>
              <div class="uk-flex uk-flex-middle uk-text-muted uk-text-small">
                <span uk-icon="icon: laptop; ratio: 1"></span>
                {{ setting.author.job }}&nbsp;
                <a href="{{ setting.author.company.link }}">
                  @{{ setting.author.company.name }}
                </a>
              </div>
              <div class="uk-flex uk-flex-middle uk-text-muted uk-text-small">
                <span uk-icon="icon: location; ratio: 1"></span> {{ setting.author.location }}
              </div>
            </div>
          </div>
          <p class="uk-flex uk-flex-center uk-flex-left@m">
            {{{ setting.author.bio }}}
          </p>
        </div>
        <div class="uk-flex uk-flex-center uk-margin-medium-top uk-margin-medium-bottom uk-navigation-social">
          <div class="uk-grid uk-grid-small" uk-grid>
            {{ #navigation.social }}
            <a href="{{ link }}" class="uk-icon-link" uk-icon="icon: {{ icon }}"></a>
            {{ /navigation.social }}
          </div>
        </div>
    </div>
  </div>
</body>
</html>